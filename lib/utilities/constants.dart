import 'package:flutter/material.dart';

//Constant colors for Cards in InputPage
const Color activeCardColor = Color(0xFFFFFFFF);
const Color inActiveCardColor = Color.fromARGB(255, 82, 83, 85);

//Constant textStyles for texts and Numbers all across
const textStyle =
    TextStyle(fontFamily: 'bold', fontSize: 20.0, color: Colors.white);
const numStyle =
    TextStyle(fontFamily: 'bold', fontSize: 40.0, color: Colors.white);
